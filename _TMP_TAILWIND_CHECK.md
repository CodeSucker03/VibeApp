Tailwind v4 setup check notes:
- tailwindcss dependency exists (v4.x)
- postcss config uses @tailwindcss/postcss
- app/globals.css currently uses Geist font vars and inline @theme; needs to be replaced with JobPilot ui-tokens @theme definition and remove inline placeholder vars

